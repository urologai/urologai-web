# UrologAI Web

Public web repository for **UrologAI — The Living Urology Encyclopedia**.

This initial B01 checkpoint contains governance and cross-agent coordination metadata only. It has no application implementation, clinical content, deployment workflow, secrets, or donor source. Later blocks will add the versioned public contracts and web application under the accepted build plan.

Canonical public hostname: `urologai.org`.

The existing site is a quarantined nonclinical prototype until a later release block explicitly replaces it.
